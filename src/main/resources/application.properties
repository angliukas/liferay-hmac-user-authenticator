# Database configuration (overridable via env vars or JVM system properties).
db.url=
db.username=
db.password=

# Optional: specify driver class or omit to infer from the JDBC URL.
db.driver=

# Email configuration (overridable via env vars or JVM system properties).
mail.host=
mail.port=25
mail.username=
mail.password=
mail.from=
mail.smtp.auth=
mail.smtp.starttls.enable=true
mail.smtp.ssl.enable=false
